# Session Handoff

**Updated:** 2026-06-08
**Branch:** feat/postgres-migration (off `main`) — committed, **not merged** (per instruction)

---

## Current State

**Postgres migration COMPLETE — Prompt 1 (port) + Prompt 2 (Alembic baseline + FK rework).**
Full suite green on real Postgres (testcontainers): **275 passed, 1 deselected, 78.71% cov**;
ruff + mypy + schema-drift pass. The production migration path is now valid → branch is
merge-ready.

### Prompt 2 deliverables
- **Full Alembic initial baseline** (`alembic/versions/0001_initial_initial_schema.py`,
  `revision=0001_initial`, `down_revision=None`): autogenerated, creates all 14 tables in FK-dependency
  order (discovery_runs/users/jobs/profiles before analyses/campaign_jobs/contacts/…). Replaced the
  old partial `0001_add_campaign_jobs` revision. All timestamps `timestamptz`.
- **`alembic upgrade head` on a clean Postgres verified** — creates the full schema (the merge-blocker
  is fixed). Alembic is the source of truth for real app migrations; `create_all` is kept only for the
  test schema bootstrap.
- **Migration test restored** (`tests/test_migrations.py`): spins its own clean `postgres:16`
  container, runs `command.upgrade(head)`, asserts all tables exist (jobs + campaign_jobs prove
  ordering). Runs in the suite (no longer skipped).
- **FK enforcement reworked per your direction**: enforcement is **ON by default**; conftest seeds the
  auth user so its owned rows resolve; `@pytest.mark.relaxed_fks` is the registered opt-out (sets
  `session_replication_role = replica` for that test only); `tests/factories.py` provides
  `make_user/make_profile/make_analysis/make_discovery_run/make_job` (seed valid parents + flush).
  **All affected tests were converted to factories/seeding — `relaxed_fks` is currently used by zero
  tests** (kept as the sanctioned escape hatch). Production never disables FK checks.

### Prompt 1 recap (carried)
asyncpg engine (+ `DB_SSL` connect_args for managed PG); `docker-compose` postgres:16; `init_db` =
`create_all` only; tz-aware columns → `timestamptz`; session-scoped testcontainers + external-txn
isolation (`join_transaction_mode="create_savepoint"`); ~19 per-file SQLite engines centralized into
conftest; `.env`/`.env.example` updated. Zero SQLite references remain. discovery.py correlated
subquery survived Postgres untouched.

## Next Action

Branch is merge-ready (Alembic path valid, migration test green, FK enforced). Merge
`feat/postgres-migration` → `main` + push **on your go**. Optional follow-up: decide whether
`init_db`/`create_all` should give way to `alembic upgrade head` at app startup (currently startup
uses create_all; Alembic is for explicit migrations).

## Open Questions

1. Merge to `main` + push — when? (Held per your instruction.)
2. Startup schema: keep `create_all` in `init_db`, or switch to `alembic upgrade head`?
3. `feat/job-board-scrapers` + `feat/referral-clean` still linger from the earlier cleanup.

## Verification Baseline

| Check | Result |
|---|---|
| `pytest` (real Postgres, FK enforced) | ✓ 275 passed, 1 deselected, 78.71% cov |
| ruff / mypy / schema-drift | ✓ all pass (45 files) |
| `alembic upgrade head` (clean PG) | ✓ full schema, dependency-ordered |
| `relaxed_fks` opt-outs in use | 0 (all tests enforce FKs via factories) |
| SQLite references | none |
