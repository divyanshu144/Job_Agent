# JobFit Agent — Project Context

## Project Overview

JobFit Agent is an AI-powered job application assistant. Users paste a job description and receive a fit score, skill gap analysis, and a tailored cover letter and resume — generated by a six-agent pipeline (job_parser → match_scorer → gap_analyst in Phase 1, then resource_planner + cover_letter + resume_tailorer concurrently in Phase 2) streamed to the browser over SSE. Tailored resumes render to DOCX and to PDF via a LaTeX/`pdflatex` toolchain. A background discovery pipeline pulls postings from multiple sources (HackerNews, Remotive, Reed, Adzuna, WorkAtAStartup, plus ATS boards for target companies), scores them with the same agents (bulk-tiered to Haiku, batched via the Anthropic Batch API), and surfaces them in a ranked feed. A cold email workflow discovers company contacts via Hunter.io and drafts personalised outreach using a seventh agent (and can send via Gmail). A multi-tenant campaign system runs nightly per-user application campaigns on Celery (worker + beat), with per-user spend caps. The app is multi-user: cookie-JWT auth, bcrypt, invite tokens, and an admin role; each candidate profile is built per user from YAML + an uploaded CV, with semantic memory (pgvector + OpenAI embeddings) over profile content.

**Stack:** Python 3.11 · FastAPI · Pydantic v2 · SQLAlchemy 2.0 (async) · **PostgreSQL** (asyncpg) + **pgvector** · Alembic · **Redis + Celery** (worker + beat) | React 19 · Vite · TypeScript · Tailwind CSS · shadcn/ui | Anthropic SDK (`claude-sonnet-4-6` / Haiku tiers) · OpenAI embeddings (`text-embedding-3-small`) | Docker Compose · local Kubernetes · AWS ECS (SQLite remains only in parts of the test suite)

---

## Common Commands

```bash
make check        # fmt + lint + test in sequence — THE command for "is my work done"
make run          # start backend (uvicorn :8000) + frontend dev server (:5173) concurrently
make fmt          # ruff format
make lint         # ruff check + mypy + pydantic→TS schema drift check
make test         # pytest --cov --cov-fail-under=70
make docker-up    # docker-compose up --build
```

Install deps:
```bash
cd backend && pip install -r requirements.txt
cd frontend && npm install
```

---

## Architecture Map

```
backend/
├── main.py          # FastAPI entrypoint, CORS, router includes, /health, /metrics, rate limiter
├── config.py        # Settings singleton (pydantic-settings); only place .env is read
├── database.py      # async engine (asyncpg), SessionLocal, init_db()
├── models.py        # SQLAlchemy ORM: User, Profile, Analysis, JobResult, CampaignRun,
│                    #   TargetCompany, LLMCall, PipelineEvent, MemoryChunk, ...
├── schemas.py       # Pydantic v2 request/response schemas + PriorOutputs
├── celery_app.py    # Celery app + beat_schedule (nightly campaign dispatch)
├── tasks.py         # Celery tasks: health.ping/db_probe, campaign.run_user_campaign,
│                    #   campaign.dispatch_nightly (beat → enqueue per eligible user)
│
├── agents/          # one BaseAgent subclass per prompt; 7th is cold_email_agent
│   ├── base.py            # shared async Claude client, prompt loader, _inject(), retry/breaker
│   ├── job_parser.py · match_scorer.py · gap_analyst.py
│   ├── resource_planner.py · cover_letter.py · resume_tailorer.py
│   ├── cold_email_agent.py   # route-driven leaf agent (manual .replace slots)
│   └── prompt_versions.py    # prompt version registry
│
├── services/
│   │  # — core analysis pipeline —
│   ├── orchestrator.py       # two-phase pipeline runner + SSE event emitter
│   ├── context_builder.py    # builds compact profile + prior-outputs prompt context
│   ├── profile_builder.py    # merges YAML + CV into Profile; compact profile; content hash
│   ├── cv_parser.py          # pypdf text extraction (run_in_executor)
│   ├── job_result.py         # one-row-per-(analysis, agent) JobResult upsert invariant
│   ├── pipeline_errors.py    # error boundary → user-safe messages
│   │  # — resume rendering —
│   ├── resume_docx.py        # structured resume content → DOCX
│   ├── resume_latex.py       # LLM-tailored LaTeX → pdflatex PDF, self-correction retry
│   ├── resume_latex_template.py  # template-based LaTeX → PDF
│   │  # — discovery + sources —
│   ├── discovery.py          # discovery pipeline runner (fetch → score → feed)
│   ├── stage2.py             # typed contract for Stage-2 relevance classification
│   ├── batch_processor.py    # Anthropic Batch API for bulk Stage-2 relevance checks
│   ├── job_shortlist.py      # shortlist/ranking helpers for the feed
│   ├── hn_client.py · remotive_client.py · reed_client.py · adzuna_client.py
│   ├── workatastartup_client.py · ats_client.py · targets_client.py  # source/ATS clients
│   │  # — campaigns (multi-tenant) —
│   ├── campaign_run.py       # regular-tier campaign run orchestration
│   ├── campaign_orchestrator.py  # score a job for the candidate (job_parser → match_scorer)
│   ├── campaign_user.py      # headless per-user campaign driver
│   ├── usage.py              # per-user LLM spend tally + campaign cap checks
│   │  # — contacts / outreach —
│   ├── contact_discovery.py  # Hunter.io contact discovery
│   ├── gmail_service.py      # server-side Gmail send/draft via OAuth refresh token
│   │  # — memory + cost + infra —
│   ├── memory.py             # semantic memory chunks (pgvector/OpenAI embeddings + keyword fallback)
│   ├── cost_calculator.py    # USD cost incl. prompt-cache tokens
│   ├── instrumentation.py    # tracked_call: LLMCall logging + Prometheus llm_calls_total
│   ├── auth_service.py       # register/login, bcrypt, JWT cookie, invite tokens
│   └── rate_limit.py         # slowapi limiter (per-user JWT key, IP fallback)
│
├── routes/
│   ├── profile.py    # GET /api/profile, POST /api/profile/refresh, /cv
│   ├── analyse.py    # POST /api/analyse (SSE), POST /api/analyse/generate/{id}
│   ├── history.py    # GET /api/history, GET /api/analysis/{id}, resume DOCX/PDF download
│   ├── contacts.py   # GET /api/contacts, POST /contacts/discover, /{id}/draft, /{id}/send
│   ├── discovery.py  # POST /api/discovery/run, /run/all; GET /runs, /sources, /feed, /saved; PATCH /jobs/{id}/save
│   ├── campaign.py   # POST /api/campaign/run-now, GET /campaign/runs (regular-tier, Celery)
│   ├── targets.py    # GET/POST/PATCH/DELETE /api/targets (target companies CRUD)
│   ├── feedback.py   # POST /api/feedback (user feedback capture)
│   ├── auth.py       # register/login/logout/invite, /me, forgot/reset password
│   └── metrics.py    # GET /api/metrics/costs/summary, /runs (admin)
│
├── evals/            # validators.py (output validation → validation_warnings), consistency_check.py
└── prompts/          # versioned prompt templates (.md); one per agent

frontend/src/
├── App.tsx
├── pages/
│   ├── ProfileSetup.tsx   # CV upload, read-only YAML viewer
│   ├── Dashboard.tsx      # landing/overview
│   ├── AnalyseJob.tsx     # JD paste, AgentProgress, navigates to /results/:id on done
│   ├── Results.tsx        # Tabbed: Score | Gaps | Resources | Letter | Resume | Cold Email
│   ├── MyResults.tsx      # user's saved analyses/materials
│   ├── Discover.tsx       # Discovery feed with filters and save actions
│   ├── Saved.tsx          # Saved jobs list
│   ├── Campaign.tsx       # run-now + run history + targets CRUD (all authenticated users)
│   ├── Costs.tsx          # LLM cost dashboard (admin only)
│   ├── AdminInvites.tsx   # admin invite management
│   ├── Login.tsx · Register.tsx · ForgotPassword.tsx · ResetPassword.tsx  # auth
├── components/
│   ├── AgentProgress.tsx  # 6 fixed-order slots; Phase 2 shows concurrent spinners
│   ├── ScoreCard.tsx · GapList.tsx · ResourcePanel.tsx
│   └── DocViewer.tsx      # markdown preview + copy/download
├── api/client.ts          # typed fetch wrappers + EventSource SSE dispatcher
└── types/                 # TS types mirroring backend schemas 1:1 (schema-drift checked)
```

---

## Key Conventions

### Config
- Import `from backend.config import settings` — the singleton is instantiated once at module load.
- Never read `os.environ` directly anywhere outside `config.py`.

### DB Sessions
- Always inject the session as a FastAPI dependency: `db: AsyncSession = Depends(get_db)`.
- Never construct `AsyncSession` manually in a route or service.
- `init_db()` is called once in `main.py` lifespan.

### Services
- Services are plain async functions or thin classes — no global state.
- Route handlers call services; services call agents; agents call Claude. No layer skipping.

### API Prefix
- Always use `settings.api_prefix` (default `"/api"`). Never hardcode `/api` in route definitions.
- Register all routers in `main.py` with `prefix=settings.api_prefix`.

### Async
- All I/O is `async` — SQLAlchemy, httpx, Anthropic SDK.
- CPU-bound work (e.g., pypdf extraction on large files) goes in `asyncio.get_event_loop().run_in_executor(None, ...)`.
- Never call blocking functions directly in an async path.

### Undocumented Conventions

**SSE event protocol** — `orchestrator.py` emits events in a fixed order: `pipeline_start → agent_start → agent_done (repeat per agent) → pipeline_done`. On agent failure, `pipeline_error` is emitted instead of `agent_done` for that agent, then execution continues. `pipeline_done` is always the final event. The frontend (`api/client.ts`) hardcodes these event names — changing them is a breaking change.

**`_inject()` vs manual `.replace()`** — `BaseAgent._inject(template, profile, jd, prior)` is for pipeline agents that consume structured `PriorOutputs` (job_parser, match_scorer, gap_analyst, resource_planner, cover_letter, resume_tailorer). Agents called directly from routes with different input shapes (e.g., `ColdEmailAgent` needs `contact_name` and `contact_title`) use manual `template.replace("{slot}", value)` chains instead. Do not force leaf agents into `_inject()`.

**`with_tracking()` pattern** — agents are instantiated fresh per request and chained with `.with_tracking(db, analysis_id=...)` to attach a DB session for cost logging. This mutates the instance in place. Never cache or reuse agent instances across requests — the previous request's DB session and `run_id` will persist silently.

**Runtime model override** — `orchestrator.py` mutates `agent.model` at dispatch time (`agent.model = HAIKU`) to tier models by pipeline phase. This is intentional: agents default to Sonnet, but the orchestrator can downgrade to Haiku for bulk/cheap operations (discovery scoring). Do not read `agent.model` as a static property; its value at call time depends on who dispatched it.

**JD hash caching** — `orchestrator.py` caches Phase 1 results keyed on `sha256(jd_text + "::" + sha256(merged_profile))` via the single `analysis_cache_key(jd, profile)` helper (`profile_content_hash` lives in `profile_builder.py`). The key is content-addressed, **not** keyed on `profile.id` (which rotates on every `build_profile`). Consequences: a Refresh/CV-upload that produces **identical** merged content keeps existing cache hits (no needless recompute); a build that **changes** content yields a new key (correct invalidation). Caveat: it keys on the *built* `merged_profile`, so editing `candidate_profile.yaml` on disk without a Refresh returns the prior result — the profile row wasn't rebuilt.

**`validation_warnings` is a meta field** — every agent output model carries `validation_warnings: list[ValidationWarning] = Field(default_factory=list, exclude=True)`. `exclude=True` strips it from `model_dump()`, so it never enters `PriorOutputs` or gets injected into downstream prompts. It lives only in memory during a request and is surfaced via `logger.warning()` calls in `backend/evals/validators.py`.

---

## Workflow Orchestration

1. **Plan Mode Default** — enter plan mode for ANY non-trivial task (3+ steps or architectural decisions). If something goes sideways mid-implementation, STOP and re-plan rather than patching forward.

2. **Subagent Strategy** — use subagents liberally to keep the main context window clean. Offload research, codebase exploration, and parallel analysis to subagents. The main context is for decisions and writing code, not for grepping.

3. **Self-Improvement Loop** — after ANY correction, immediately update `tasks/lessons.md` with the pattern that caused the error. Review `tasks/lessons.md` at the start of every new session before touching code.

4. **Verification Before Done** — never mark a task complete without proving it works: run the relevant tests, check logs, demonstrate correctness. "It should work" is not verification.

5. **Autonomous Bug Fixing** — when given a bug report, just fix it. No permission needed. Reproduce → Locate → Fix → Verify. Use the `debug-playbook` skill.

---

## Task Management

1. Before starting any implementation, write the plan to `tasks/todo.md` as a checklist.
2. Check in with the user before beginning implementation (not during).
3. Mark items complete (`- [x]`) as you finish each one — do not batch.
4. After any correction or unexpected discovery, add an entry to `tasks/lessons.md`.
5. Update `HANDOFF.md` at every checkpoint (see Session Lifecycle below).

---

## Session Lifecycle

> **Checkpoint triggers** — update `HANDOFF.md` whenever: task complete · wave/milestone done · user signals stop · unresolved blocker · context window ~70% · 30+ min without an update

### At session start

1. Read `HANDOFF.md` — resume from last known state; confirms next action and in-flight files.
2. Read `tasks/todo.md` — verify task checklist matches HANDOFF current state.
3. Read `tasks/lessons.md` (chronological "what broke / what we learned" log) and `tasks/agent_memory.md` (durable structured reference: Architecture Decisions, Known Gotchas, Solved Problems, Useful Patterns) before touching code. Honour locked Architecture Decisions even if surrounding code suggests otherwise.
4. Run `git status` and `git log -1` — verify in-flight files match HANDOFF.md's In-Flight section, and the last commit hash/message matches the expected state. If they diverge, treat the working tree as authoritative and update HANDOFF.md before proceeding.

### At each checkpoint

1. Mark completed tasks in `tasks/todo.md`.
2. Overwrite `HANDOFF.md` with current state (use `HANDOFF.template.md` as reference).
3. Add an entry to `tasks/lessons.md` if a non-obvious pattern was encountered.
4. After any significant discovery, append to the relevant section of `tasks/agent_memory.md` (problem / root cause / solution). lessons.md is the chronological log; agent_memory.md is the structured reference — don't duplicate.

### At session end

1. Write final `HANDOFF.md` — "Next Action" must be immediately actionable for a cold-start session.
2. Confirm `tasks/todo.md` reflects all completed and remaining work.
3. Record any deferred decisions or unresolved questions in the Open Questions section of `HANDOFF.md`.

---

## Definition of Done

A task is not complete until all of the following are true:

- `make check` passes (`fmt` + `lint` + `test` in sequence)
- Every new agent has a test in `tests/test_agents/` that validates its output schema against a mocked `_call()` response
- Every new route has at least one integration test in `tests/test_routes/` covering the happy path and auth requirement
- `HANDOFF.md` reflects the current state (updated at this checkpoint)

"It should work" is not verification. Run the checks.

---

## Core Principles

- **Simplicity First** — make every change as small and simple as possible. Minimal code impact. If a simpler approach exists, take it.
- **No Laziness** — find root causes. No temporary fixes, no workarounds that leave debt. Hold senior developer standards.
- **Minimal Impact** — only touch files that are necessary for the task. Avoid opportunistic refactoring unless it's directly in the path of the change.


## Tools
- Use Graphify (`.code-review-graph`) for codebase navigation before exploring files
- Check Supermemory at session start for persisted context

## Env
- Copy `.env.example` → `.env`, add ANTHROPIC_API_KEY