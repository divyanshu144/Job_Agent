version: "3.9"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      PROFILE_YAML_PATH: /app/data/candidate_profile.yaml
      CV_PATH: /app/data/cv.pdf
    volumes:
      - ./data:/app/data
